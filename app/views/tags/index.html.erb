<h1>All Tags</h1>

<ul id="tag">
    <% @tags.each do |tag| %>
      <li>
        <%= link_to tag.name, tag_path(tag)   %>
      </li>
    <% end %>
  </ul>
<%= link_to "Hompage",articles_path, class: "new_article" %>
